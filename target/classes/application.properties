spring.application.name=sistur

# Datasource - configurado via variáveis de ambiente no Render
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres?prepareThreshold=0&sslmode=require}
spring.datasource.username=${DB_USERNAME:postgres.kwddavsummldocvmafgn}
spring.datasource.password=${DB_PASSWORD:a4oTJLl4fUUzP2Dc}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database=postgresql
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Logging (menos verboso em produção)
logging.level.org.springframework.web=INFO
logging.level.br.gov.noronha.sistur=INFO

# Google Auth
google.client.id=${GOOGLE_CLIENT_ID:10352554378-pk9fqug556o9qbeuom8ofom7o3u9vj0t.apps.googleusercontent.com}

# JWT Secret
api.security.token.secret=${JWT_SECRET:my-secret-key}
